import React from 'react';
// import PropTypes from 'prop-types';

const Home = () => (
  <div>
    <h1>Marvel Movie Database (stateless)</h1>
  </div>
);

// Home.propTypes = {
//   : PropTypes.
// };

export default Home;
